import { QuantityMultiplierPipe } from './quantity-multiplier.pipe';

describe('QuantityMultiplierPipe', () => {
  it('create an instance', () => {
    const pipe = new QuantityMultiplierPipe();
    expect(pipe).toBeTruthy();
  });
});
